/*
const loginMenuContainer = document.querySelector(".loginMenu-container");
const logoutMenuContainer = document.querySelector(".logoutMenu-container");

loginMenuContainer.addEventListener("mouseenter", () => {
    loginMenuContainer.style.background = "black";
    loginMenuContainer.style.transition = "color 2s ease-in-out";
});
loginMenuContainer.addEventListener("mouseleave", () => {
    loginMenuContainer.style.background = "white";
    loginMenuContainer.style.transition = "color 2s ease-in-out";
});

logoutMenuContainer.addEventListener("mouseenter", () => {
    logoutMenuContainer.style.background = "black";
    logoutMenuContainer.style.transition = "color 2s ease-in-out";
});
logoutMenuContainer.addEventListener("mouseleave", () => {
    logoutMenuContainer.style.background = "white";
    logoutMenuContainer.style.transition = "color 2s ease-in-out";
});
*/
